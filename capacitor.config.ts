import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.lovemap',
  appName: 'LoveMap',
  webDir: 'dist/findlove/browser'
};

export default config;
